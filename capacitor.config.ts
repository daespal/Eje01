import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Eje01_GaleriaImg',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
